# psk_config.cfg — Pre-shared key configuration for DTLS baseline experiment.
#
# This file is read by run_trials.sh, which passes the values as CLI arguments
# to dtls_server and dtls_client.  The C programs do NOT read this file directly.
#
# SECURITY NOTE: This is a fixed test PSK for a benchmarking experiment only.
# Never use this key in a production system.

PSK_IDENTITY=exo-dtls-client
PSK_HEX=deadbeef0102030405060708090a0b0c

# Server network configuration
SERVER_PORT=4433

# Experiment parameters (used by run_trials.sh)
TRIALS=500
INTER_TRIAL_MS=250
TIMEOUT_MS=5000
